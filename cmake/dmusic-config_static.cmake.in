@PACKAGE_INIT@

get_filename_component(dmusic_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
list(APPEND CMAKE_MODULE_PATH ${dmusic_CMAKE_DIR})

find_package(sf2cute REQUIRED)
find_package(SndfileStatic REQUIRED)

if(NOT TARGET dmusic::dmusic)
  include(${CMAKE_CURRENT_LIST_DIR}/dmusic-targets.cmake)
endif()

set(DMUSIC_LIBRARIES dmusic::dmusic)